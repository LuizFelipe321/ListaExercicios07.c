#include <stdio.h>

int main() {
    char jogadorCirculo[3]; // 3 jogadas para circulo
    char jogadorXis[3];     // 3 jogadas para o xizinho
    char tabuleiro[3][3];   // tabuleiro
    int vencedor = 0;

    printf("Nome do jogador da bolinha:");
    scanf("%s", jogadorCirculo);

    printf("Nome do jogador Xizinho: ");
     scanf("%s", jogadorXis);

    //leitura do tabuleiro , preenchimento com O ou X
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
         printf("Preencha o tabuleiro com Xis ou bolinha:\n");
         scanf(" %c", &tabuleiro[i][j]);
        }
    }

    // se preenchimento for O na linha
    for (int i = 0; i < 3; i++) {
        if (tabuleiro[i][0] == 'O' && tabuleiro[i][1] == 'O' && tabuleiro[i][2] == 'O') {
         printf("%s venceu!\n", jogadorCirculo);
         vencedor = 1;
    }
        // se preenchimento for X
        if (tabuleiro[i][0] == 'X' && tabuleiro[i][1] == 'X' && tabuleiro[i][2] == 'X') {
         printf("%s venceu!\n", jogadorXis);
         vencedor = 1;
       }
      }

    for (int j = 0; j < 3; j++) {
        if (tabuleiro[0][j] == 'O' && tabuleiro[1][j] == 'O' && tabuleiro[2][j] == 'O') {
         printf("%s venceu!\n", jogadorCirculo);
         vencedor = 1;
     }

        if (tabuleiro[0][j] == 'X' && tabuleiro[1][j] == 'X' && tabuleiro[2][j] == 'X') {
         printf("%s venceu!\n", jogadorXis);
         vencedor = 1;
      }
    }

    if (tabuleiro[0][0] == 'O' && tabuleiro[1][1] == 'O' && tabuleiro[2][2] == 'O') {
        printf("%s venceu!\n", jogadorCirculo);
        vencedor = 1;
    }

    if (tabuleiro[0][0] == 'X' && tabuleiro[1][1] == 'X' && tabuleiro[2][2] == 'X') {
         printf("%s venceu!\n", jogadorXis);
        vencedor = 1;
    }

    if (tabuleiro[0][2] == 'O' && tabuleiro[1][1] == 'O' && tabuleiro[2][0] == 'O') {
        printf("%s venceu!\n", jogadorCirculo);
        vencedor = 1;
    }

    if (tabuleiro[0][2] == 'X' && tabuleiro[1][1] == 'X' && tabuleiro[2][0] == 'X') {
        printf("%s venceu!\n", jogadorXis);
        vencedor = 1;
    }

    if (!vencedor) { // ocorre o empate caso nenhum dos jogadores seja o vencedor
        printf("Deu velha!\n"); //imprime na tela
    }

    return 0;// encerra o programa
}
